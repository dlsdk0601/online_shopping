export enum UserSearchType {
  NAME = "NAME",
  PHONE = "PHONE",
}

export enum UserType {
  LOCAL = "LOCAL",
  GOOGLE = "GOOGLE",
  APPLE = "APPLE",
  KAKAO = "KAKAO",
  NAVER = "NAVER",
}

export enum SubscribeSearchType {
  EMAIL = "EMAIL",
  NAME = "NAME",
}
